{
  "name": "example-electron",
  "version": "1.0.0",
  "description": "",
  "main": "Main.ts",
  "author": {
    "name": "Gikkman",
    "url": "http://www.github.com/gikkman"
  },
  "scripts": {
    "build": "rm -rf output && tsc && rollup --bundleConfigAsCjs -c && copyfiles -u 1 frontend/*.html frontend/*.css output/",
    "start": "electron -r ts-node/register ."
  },
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^24.0.0",
    "rollup": "^3.20.2"
  }
}
